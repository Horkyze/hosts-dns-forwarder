# Configuration for hosts-dns-forwarder.
#
# After editing, apply with:
#   systemctl restart hosts-dns-forwarder

# Listen address (use :53 for system resolver usage)
HOSTS_DNS_FORWARDER_LISTEN=127.0.0.1:53

# TTL for answers sourced from /etc/hosts
HOSTS_DNS_FORWARDER_TTL=60

# Upstream UDP timeout in milliseconds
HOSTS_DNS_FORWARDER_TIMEOUT_MS=1500

# Hosts file path
HOSTS_DNS_FORWARDER_HOSTS=/etc/hosts

# Optional resolv.conf override.
# If unset, the wrapper prefers /run/systemd/resolve/resolv.conf when present,
# otherwise falls back to /etc/resolv.conf.
# HOSTS_DNS_FORWARDER_RESOLV=/etc/resolv.conf

# Optional upstream override (space-separated).
# If set, these override nameservers parsed from resolv.conf.
# Examples:
#   HOSTS_DNS_FORWARDER_UPSTREAMS=127.0.0.53
#   HOSTS_DNS_FORWARDER_UPSTREAMS="1.1.1.1 9.9.9.9"
#   HOSTS_DNS_FORWARDER_UPSTREAMS="[2001:4860:4860::8888]:53"
HOSTS_DNS_FORWARDER_UPSTREAMS=

# Extra args (optional, split on whitespace)
HOSTS_DNS_FORWARDER_EXTRA_ARGS=
