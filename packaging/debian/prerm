#!/bin/sh
set -e

case "$1" in
  remove|upgrade|deconfigure)
    if command -v systemctl >/dev/null 2>&1 && [ -d /run/systemd/system ]; then
      systemctl stop hosts-dns-forwarder.service >/dev/null 2>&1 || true
    fi
    ;;
esac

exit 0
